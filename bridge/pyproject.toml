[build-system]
requires = ["setuptools >= 77"]
build-backend = "setuptools.build_meta"

[project]
name = "wbl_bridge"
version = "1.0.0"
dependencies = [
    "transformers>=4.51.0", 
    "torch>=2.5.0",
]
requires-python = ">=3.8"
description = "HF Model Definition/Megatron-bridge code for NC AI WBL Model."
